use_helpdesk_chat:
  name: Activer la messagerie instantannée de support
  default: true
  type: boolean

anonymous_helpdesk_chat:
  name: Activer la messagerie instantannée de support même si l'utilisateur n'est pas connecté
  default: true
  type: boolean
  
legacy_auth_enabled:
  name: "Afficher la page d'authentification interne aux utilisateur"
  default: true
  type: boolean

max_actives_era:
  name: Nombre max de redirections actives 
  default: 2
  type: integer

default_mail_domains:
  name: Domaines pour lequels les adresses mails sont créée par défaut lors de l'inscrption. Il n'est pas necessaire de spécifier les alias de domaine (voir admin/domaines). Domaines séparés par les espaces.
  default: gadz.org m4am.net