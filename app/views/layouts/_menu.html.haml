%header
  .int
    %nav
      #logo.left
        =image_tag "logo.png", :class => "vertical-center"
      .menu
        %ul
          - if user_signed_in?
            - if can? :read, :admin
              %li
                =link_to "Administration", admin_path
            %li
              =link_to "Menu 1", :back

          = render 'layouts/user_widget'