%p#notice= notice

%h1 Alias
%p
  %b Origine:
  = @alias.to_s
%p
  %b Destination:
  = @alias.redirect

%p
  -if @alias.ml_list.present?
    %b Liste de diffusion:
    =link_to @alias.ml_list.name, @alias.ml_list
  -else
    %b Pas de liste de diffusion


= link_to 'Edit', edit_alias_path(@alias)
\|
= link_to 'Back', aliases_path
